# دليل HipHopLord — نسخة نهائية ببيانات حقيقية

هذه النسخة تحتوي الآن على **439 بطاقة حقيقية** مستخرجة من بياناتك (SoundCloud، Fiverr، Google Maps)، بعد تنقية وتصنيف يدوي/برمجي لضمان الجودة — وليس نقلاً حرفياً للبيانات الخام.

## تحديث: عدد المنتجين وروابط Fiverr

- **عدد المنتجين (Producers): 21 الآن (بدءاً من 4).** ثلاث مراحل: (1) تصحيح فلتر "beat signal" في build_directory.py (أضافت "boom bap" كمؤشر) رفع العدد من 4 إلى 6. (2) فحص ملف الـ400 تراك الإضافي الذي زوّدتني به (40 حساب SoundCloud)، بمراجعة يدوية لكل حساب ميّزت 7 منتجين حقيقيين جدد واستبعدت 4 حسابات كانت لتمر بالخطأ (Emad Saad Hip Hop رابر، Dgunzbeatz مهندس مكساج، Lance O Smith حساب ريمكسات، MagroTheHipHopArtist رابر) — رفع العدد إلى 13. أسباب كل استبعاد موثّقة كتعليق داخل `build_directory.py` (EXCLUDED_PRODUCER_USERNAMES). (3) اكتشاف عبر بحث جوجل المباشر (`site:soundcloud.com "type beat" "prod by"` وعبارات مشابهة)، تحقّقتُ يدوياً من كل حساب عبر صفحته الحقيقية على SoundCloud (البايو، عدد المتابعين، أرقام استماع/إعجاب فعلية لكل تراك) قبل إضافته — 8 منتجين جدد حقيقيين من بلجيكا ونيجيريا والبرازيل وغانا وريو دي جانيرو، موثّقون في `MANUAL_PRODUCER_ADDITIONS` داخل السكربت مع مصدر كل واحد. النتيجة النهائية: 21 منتجاً حقيقياً بالكامل، بدون أي بطاقة ملفّقة.
- **روابط Fiverr (Engineers/Visuals) — تم حلها بالكامل: 120 من أصل 121 رابطاً (البطاقة المتبقية حُذفت لأن الخدمة نفسها لم تعد موجودة على Fiverr).** بعد أن اتضح أن Fiverr لا يقبل الروابط المُعاد بناؤها برمجياً مهما كانت صحيحة الصيغة، أرسلتُ جدول Excel بـ121 رابط خدمة، وأعاد المستخدم توليدها يدوياً من لوحة تحكم Fiverr الحقيقية (أداة "LP URL"). الجولة الأولى أعادت 113 رابطاً صحيحاً مباشرة، و8 روابط بها مشاكل (خطأ إملائي في 2، رابط بدون تتبع أفلييت في 5، وبائع مختلف تماماً في واحدة) — أرسلتُ للمستخدم جدولاً مصغّراً بالثمانية فقط مع شرح كل مشكلة، وأعاد 7 منها مصححة، واكتشف أن الخدمة الثامنة (`vis-074`) لم تعد موجودة على Fiverr فحذفها. **النتيجة النهائية: 120 رابط أفلييت صحيح ومُتحقَّق منه، وبطاقة واحدة (vis-074) محذوفة نهائياً من الموقع.** كل هذا مدمج في `directory.json` ومحفوظ أيضاً بشكل دائم داخل `build_directory.py` (قاموس `MANUAL_FIVERR_LINK_OVERRIDES` للروابط + مجموعة `REMOVED_LISTING_IDS` للبطاقة المحذوفة) بحيث لا يُفقَد أي من هذا عند أي إعادة توليد مستقبلية للموقع.

## تحديث: إثراء محتوى البطاقات (بيانات حقيقية لم تكن مستغلة)

فحصتُ ملفات البيانات الخام الثلاثة (SoundCloud، Fiverr، Google Places) ووجدت حقولاً حقيقية ودقيقة لم تكن تظهر في البطاقات رغم توفرها أصلاً — تمت إضافتها الآن دون أي تلفيق أو أرقام مصطنعة:

- **المنتجون:** عدد المتابعين الحقيقي، عدد مرات الاستماع الفعلي، تاريخ رفع التراك، وشارة "✓ حساب SoundCloud موثّق" إن كان الحساب verified فعلياً على SoundCloud.
- **المهندسون والمصممون (Fiverr):** مدة التسليم بالأيام، اللغات التي يتحدثها البائع، عدد صور المعرض الكامل المتاحة، وشارة "PRO Verified Seller" لبائعي Fiverr Pro المعتمدين رسمياً.
- **الاستوديوهات:** ملخص ساعات العمل الأسبوعية (مبني من بيانات جوجل الفعلية، وليس افتراضاً)، عدد الصور المتوفرة على جوجل، وشارات مرافق حقيقية (Wi-Fi، مواقف مجانية، إمكانية وصول لذوي الإعاقة) عند توفرها في بيانات "additionalInfo" من Google Places.

كل هذه الإضافات نُفِّذت داخل `build_directory.py` (دوال `format_count`، `format_upload_date`، `format_languages`، `summarize_opening_hours`، `extract_amenity_badges`) وفي عرض البطاقة داخل `index.html` (حقلا `extraStats` و`badges` الجديدان في كل عنصر بـ`directory.json`)، وتم اختبارها ببراوزر حقيقي (Playwright) على الفئات الأربع بدون أي خطأ جافاسكريبت.

## تحديث: الطبقة التحريرية "ملاحظة المحرر" (تجربة أولى على فئة المنتجين)

أضفتُ الآن حقل `curatorNote` — جملة أو جملتان تشرحان "لماذا هذه البطاقة هنا" — لكل واحد من المنتجين الـ21 (فئة Producers فقط، كتجربة أولى/pilot). كل جملة مبنية حصراً على بيانات حقيقية موجودة أصلاً في البطاقة (عدد المتابعين، عدد الاستماعات، الإعجابات، تاريخ الرفع، الموقع، الوسوم) — لا يوجد أي رقم أو ادعاء مختلَق.

راجعتُ فعلياً مستوى التفاعل الحقيقي لكل منتج قبل الكتابة، ووجدت أن بعضهم (مثل prod-006 بصفر متابع/صفر استماع/صفر إعجاب) لا يملك أي دليل حقيقي على الجودة أو الانتشار. طرحتُ عليك الخيار: إما استبعادهم، أو الإبقاء عليهم مع ملاحظة محايدة صادقة فقط — واخترت **الإبقاء وكتابة ملاحظة محايدة فقط لهم**. وهذا بالضبط ما نُفِّذ: المنتجون ذوو التفاعل القوي (مثل prod-010 بـ80.9K استماع و1448 إعجاب، أو prod-016/prod-019/prod-020/prod-013) حصلوا على ملاحظات أكثر إيجابية وتفصيلاً تعكس أرقامهم الحقيقية، بينما المنتجون ذوو التفاعل الضعيف أو المعدوم حصلوا على ملاحظات محايدة صريحة (مثال prod-006: "A newly uploaded catalog... There isn't a track record to point to...") دون أي مبالغة أو تلفيق.

التنفيذ التقني: قاموس `PRODUCER_CURATOR_NOTES` جديد داخل `build_directory.py` (بنفس نمط `MANUAL_FIVERR_LINK_OVERRIDES` — يُطبَّق بعد بناء قائمة المنتجين مباشرة في `main()` حتى لا يؤثر على ترقيم الـ IDs)، وحقل `curatorNoteBlock` جديد في دالة `cardHTML()` داخل `index.html` يعرض الملاحظة بتصميم مميز (حد ذهبي جانبي + خط مائل) تحت المشغّل الصوتي مباشرة. تم اختباره ببراوزر حقيقي (Playwright): 21 بطاقة منتج، 21 ملاحظة محرر ظاهرة بنجاح، صفر أخطاء JavaScript، وبقيت أعداد الفئات الأخرى كما هي (37/83/298).

## تحديث: ملاحظات المحرر — المهندسون والمصممون (120 بطاقة من Fiverr)

وسّعت طبقة "ملاحظة المحرر" لتشمل الآن جميع بطاقات المهندسين (37) والمصممين (83) — 120 بطاقة إجمالاً. الاستوديوهات (298 بطاقة) أُجِّلت عمداً لمرحلة لاحقة بناءً على طلبك.

نظراً لأن بيانات Fiverr منظّمة وموحّدة أكثر بكثير من بيانات SoundCloud الخام للمنتجين (تقييم رقمي، عدد مراجعات، مدة تسليم، لغات، شارة PRO — لكل بطاقة)، اخترتَ (عند سؤالك) الأسلوب المختلط:

- **طبقة أساسية آلية لكل الـ120 بطاقة:** دالة جديدة `generate_fiverr_curator_note()` في `build_directory.py` تبني جملة موجزة صادقة من بيانات كل بطاقة فعلياً (التقييم، عدد المراجعات، مدة التسليم، اللغات، شارة PRO) — لا يوجد أي رقم مختلَق، وحتى البطاقات بلا أي مراجعات تحصل على ملاحظة محايدة صريحة ("limited review history... worth checking their full profile").
- **طبقة تحريرية يدوية أعمق لـ25 بطاقة "مميزة" (featured):** وهي البطاقات التي تحمل تقييم ≥4.9★ ومراجعات ≥500 (10 مهندسين + 15 مصمماً) — كتبتُ لكل واحدة منها ملاحظة أكثر تحديداً وتفصيلاً (تبرز مثلاً السرعة، السعر، اللغات، أو الأسلوب الفني المميز)، مبنية بالكامل على أرقامها الحقيقية الخاصة. هذه الملاحظات اليدوية تُطبَّق كـ"تجاوز" (override) فوق الملاحظة الآلية عبر قاموس `FIVERR_FEATURED_CURATOR_NOTES`.

النتيجة: **120 من 120 بطاقة مهندس/مصمم لديها الآن ملاحظة محرر، بدون أي بطاقة ناقصة.** تم التحقق برمجياً (لا توجد أي بطاقة بدون `curatorNote`) وببراوزر حقيقي (Playwright): 37 بطاقة مهندس + 83 بطاقة مصمم، 120 ملاحظة ظاهرة، صفر أخطاء JavaScript، وبقي إجمالي الموقع 439 بطاقة كما هو.

## تحديث: ملاحظات المحرر — الاستوديوهات (298 بطاقة من Google Places)

أكملت الآن طبقة "ملاحظة المحرر" لتشمل جميع بطاقات الاستوديوهات الـ298 — وبهذا أصبحت الطبقة التحريرية مكتملة 100% عبر كل الموقع (439/439 بطاقة).

بيانات الاستوديوهات (Google Places) من أقوى الفئات من حيث المصداقية: عنوان حقيقي، هاتف، تقييم Google فعلي، عدد مراجعات، ساعات عمل، عدد صور، ومرافق حقيقية (Wi-Fi، مواقف، إمكانية وصول لذوي الإعاقة). طبّقت نفس الأسلوب المختلط الذي استُخدم مع المهندسين والمصممين:

- **طبقة أساسية آلية لكل الـ298 بطاقة:** دالة `generate_studio_curator_note()` جديدة تبني جملة صادقة من التقييم وعدد المراجعات والمرافق المُدرجة فعلياً — والبطاقات النادرة بلا أي مراجعة جوجل تحصل على ملاحظة محايدة صريحة بدل الفراغ.
- **طبقة تحريرية يدوية أعمق لـ66 بطاقة "مميزة" (featured):** وهي البطاقات بتقييم ≥4.8★ ومراجعات ≥100 — أبقيتُ على هذه العتبة كما هي بناءً على اختيارك، رغم أنها أكبر من ضِعفي حجم جولة المهندسين/المصممين (25). كتبتُ لكل واحدة من الـ66 ملاحظة مخصّصة تبرز ما يميزها فعلياً (عدد مراجعات استثنائي، توفر على مدار الساعة، توثيق ضخم بالصور، فئات إضافية حقيقية من Google مثل "متحف" أو "قاعة فعاليات" أو "منظمة غير ربحية")، مبنية بالكامل على بياناتها الحقيقية الخاصة. من الأمثلة اللافتة: **Sun Studio** في ممفيس (6,696 مراجعة و25,900 صورة على Google — وهو أيضاً مسجّل كمتحف ومتجر أسطوانات، ليس استوديو تسجيل تجارياً عادياً فقط)، و**Studio1212** في لاس فيغاس (844 مراجعة)، و**Bay Eight Recording Studios** في ميامي (823 مراجعة و1,400 صورة).

النتيجة: **298 من 298 بطاقة استوديو لديها الآن ملاحظة محرر، بدون أي بطاقة ناقصة.** تم التحقق برمجياً (صفر بطاقات بلا `curatorNote`) وببراوزر حقيقي (Playwright): 298 بطاقة، 298 ملاحظة ظاهرة، صفر أخطاء JavaScript، وبقي إجمالي الموقع 439 بطاقة كما هو.

**الخلاصة: طبقة ملاحظات المحرر مكتملة الآن لكل فئات الموقع الأربع — 21 منتج + 120 مهندس/مصمم + 298 استوديو = 439/439 بطاقة.**

## تحديث: المرحلة الثالثة — صفحة فردية حقيقية لكل بطاقة (439 صفحة)

نفّذت الآن ما كان مؤجَّلاً منذ بداية النقاش تحت اسم "المرحلة الثالثة": **صفحة HTML كاملة ومستقلة لكل بطاقة من الـ439**، بدل الاعتماد الكامل على تحميل البيانات عبر JavaScript فقط. هذا يعالج نقطة ضعف حقيقية في السيو كانت موجودة منذ البداية: محرك بحث لا يُنفّذ الجافاسكريبت (أو يؤخّر تنفيذه) لم يكن يرى محتوى أي بطاقة على الإطلاق، ولم يكن هناك أي رابط فريد قابل للفهرسة لكل مزوّد على حدة.

**كيف يعمل:**

- سكربت جديد `generate_listing_pages.py` يقرأ `directory.json` وينتج مجلد `listings/` يحتوي على 439 ملف HTML مستقل (مثال: `listings/prod-010.html`)، كل واحد يحمل عنوان `<title>` ووصف meta فعليَّين، رابط canonical، وسوم Open Graph/Twitter، وأهم شيء: **بيانات JSON-LD منظَّمة (Structured Data)** — من نوع "LocalBusiness" للاستوديوهات (تتضمن العنوان الحقيقي والهاتف والتقييم) ومن نوع "Service" للمنتجين والمهندسين والمصممين (مع مزوّد الخدمة والتقييم إن وُجد) — وهذا النوع من البيانات هو بالضبط ما تستخدمه جوجل لإظهار "نجوم التقييم" في نتائج البحث.
- كل صفحة تعرض المحتوى كاملاً بشكل ثابت في HTML الأصلي (الوصف، ملاحظة المحرر، الإحصائيات، الشارات، السعر، رابط CTA الخارجي) — قابل للقراءة فوراً من أي زاحف بحث بدون انتظار تنفيذ جافاسكريبت.
- أضفت قسم "More in [الفئة]" في أسفل كل صفحة يعرض 3 بطاقات أخرى حقيقية من نفس الفئة (روابط داخلية حقيقية بين الصفحات — مفيد جداً للسيو ولتوزيع "قوة" الصفحة الرئيسية على الصفحات الفردية).
- عدّلت `index.html`: عنوان كل بطاقة في الشبكة الرئيسية أصبح الآن رابطاً فعلياً يفتح صفحتها الفردية (`listings/{id}.html`)، بينما بقي زر "View on Fiverr / Listen on SoundCloud / Visit Website" كما هو تماماً (الرابط الخارجي الأساسي لتحقيق الدخل يبقى بلا تغيير).
- أعدت توليد `sitemap.xml` بالكامل ليشمل الآن 444 رابطاً (5 صفحات ثابتة + 439 صفحة بطاقة) بدل 5 فقط سابقاً — هذا يعني أن جوجل الآن لديه خريطة مباشرة لكل صفحة في الموقع بدل الاعتماد على اكتشافها عبر الزحف فقط.

**التحقق:** فحصت عيّنة من الصفحات (منتج، مهندس، مصمم، استوديو، بما فيها Sun Studio بمراجعاتها الاستثنائية) ببراوزر حقيقي (Playwright): كل صفحة تعرض العنوان والوصف وملاحظة المحرر ومسار التصفح (Breadcrumb) بشكل صحيح، بيانات JSON-LD صالحة (تم تحليلها كـJSON بنجاح)، الروابط الداخلية بين الصفحات (قسم "More in...") تعمل وتُرجع 200، وروابط عناوين البطاقات في الصفحة الرئيسية (439 رابطاً) كلها تعمل، وصفر أخطاء JavaScript في كل الحالات.

**ملاحظة تقنية:** `generate_listing_pages.py` منفصل عن `build_directory.py` (لا يحتاج بيانات خام، فقط `directory.json` الجاهز) — يجب تشغيله بعد كل مرة يُعاد فيها توليد `directory.json` حتى تبقى صفحات `listings/` متوافقة مع أحدث بيانات.

**تنبيه مهم بخصوص النشر:** هذا التحديث يضيف 439 ملفاً جديداً داخل مجلد `listings/` — عند الرفع على GitHub تأكد من رفع المجلد كاملاً وليس فقط الملفات المعتادة، وكذلك عند استخدام "New deployment" على Cloudflare تأكد من سحب المجلد الكامل (وليس فقط الملفات الفردية المعتادة) حتى تُنشر كل الصفحات الفردية بنجاح.

## تدقيق سيو فعلي للصفحات الفردية (بعد سؤال المستخدم) — نتائج وإصلاحات

بعد سؤال مباشر "هل هذه الصفحات مهيّأة من حيث السيو؟"، لم أكتفِ بالتأكيد — فحصت الـ439 صفحة برمجياً بحثاً عن مشاكل حقيقية، ووجدت وأصلحت اثنتين:

1. **رابط `rel="sponsored"` كان مطبَّقاً على كل الصفحات دون تمييز.** توصية جوجل الرسمية هي وسم الروابط المدفوعة/الأفلييت فقط بـ`rel="sponsored"`. لكن فقط روابط المهندسين والمصممين (Fiverr) هي روابط أفلييت حقيقية — روابط المنتجين تشير إلى SoundCloud وروابط الاستوديوهات تشير إلى مواقعها الرسمية، وليست مدفوعة. صححت المنطق في `generate_listing_pages.py` وفي `index.html` بحيث يُطبَّق `rel="sponsored"` فقط على فئتَي Engineers وVisuals، بينما تبقى باقي الروابط `rel="noopener"` عادية — وهذا الوضع الصحيح تقنياً.

2. **~45 صفحة (من أصل 439) كانت تتشارك نص "ملاحظة المحرر" نفسه حرفياً** — تحديداً الصفحات ذات القيم الآلية القاعدية (بطاقات بتقييم/مراجعات/شارات متطابقة تماماً)، ما قد يُقرأ كمحتوى شبه مكرر لعلامات meta description. أضفت موقع البطاقة الحقيقي (المدينة/الدولة) إلى دالتَي `generate_fiverr_curator_note()` و`generate_studio_curator_note()` في `build_directory.py` — بيانات حقيقية موجودة أصلاً في كل بطاقة لم تكن مستخدمة في الملاحظة الآلية. هذا خفض التكرار من 45 صفحة إلى 7 فقط (حالات نادرة لبطاقتين بنفس المدينة تماماً ونفس الإحصائيات تماماً).

**الخلاصة الصادقة لحالة السيو الحالية:**

- ✅ نقاط قوة حقيقية: 439 رابط فريد وقابل للفهرسة، كل صفحة تحمل `<title>` و`meta description` و`canonical` مستقلين (لا تكرار حرفي في أي `<title>`)، بيانات JSON-LD صالحة تقنياً (Service/LocalBusiness + BreadcrumbList) على كل صفحة تم التحقق منها، محتوى فعلي مكتوب مباشرة في HTML الأصلي (وليس محمَّلاً عبر JavaScript فقط كما كان الوضع سابقاً)، روابط داخلية حقيقية بين الصفحات، خريطة موقع (sitemap.xml) شاملة بـ444 رابطاً، وبنية عناوين صحيحة (H1 واحد فقط لكل صفحة + مسار تصفح Breadcrumb).
- ⚠️ ما تبقى وهو أمر طبيعي وليس خطأً: بعض عناوين `<title>` تتجاوز ~60 حرفاً (130 من 439) لأن أسماء البطاقات الحقيقية نفسها طويلة أحياناً — جوجل يقتصّها في العرض فقط، وهذا ليس خطأً تقنياً. كما أن التصميم لا يزال يعتمد على Tailwind عبر CDN (JavaScript) لتنسيق الشكل المرئي فقط — المحتوى النصي نفسه يبقى مقروءاً بالكامل من أي زاحف حتى لو تعطّل الجافاسكريبت، لكن الشكل البصري لن يظهر بدونه؛ هذا اختيار مقصود من بداية المشروع حفاظاً على البساطة دون أدوات بناء (build tools)، وموثّق أعلاه في قسم "تحسينات السيو".

تم التحقق من كل الإصلاحات ببراوزر حقيقي (Playwright) بعد التطبيق: 439/439 صفحة، صفر أخطاء JavaScript، بيانات JSON-LD صالحة على العيّنة المفحوصة، وروابط `rel="sponsored"` مطبَّقة بشكل صحيح فقط على فئتَي المهندسين والمصممين.

## تحديث: قسم "أسئلة شائعة" (FAQ) لكل بطاقة + آلية دائمة لإضافة أسئلتك الخاصة

بناءً على طلبك المباشر لتوسيع محتوى الصفحات دون حشو مصطنع (بدون هدف عدد كلمات تعسفي)، أضفت لكل صفحة من الـ439 قسم "Frequently Asked Questions" مبنياً بالكامل على بيانات حقيقية موجودة أصلاً في كل بطاقة — لا يوجد أي محتوى مُختلَق.

**كيف يعمل التوليد الآلي:** أضفت في `build_directory.py` ثلاث دوال حسب الفئة (`_faq_producer` للمنتجين، `_faq_fiverr` للمهندسين والمصممين، `_faq_studio` للاستوديوهات)، كل واحدة تفحص الحقول الحقيقية المتوفرة لتلك البطاقة تحديداً (السعر، مدة التسليم، التقييم، عدد المراجعات، الشارات، الوسوم، الموقع، ساعات العمل...) وتولّد سؤالاً وجواباً فقط إن كانت البيانات اللازمة متوفرة فعلاً — إن لم تتوفر معلومة معيّنة (مثلاً بطاقة بلا مراجعات)، لا تُختلَق إجابة، بل يُتخطى ذلك السؤال أو تُذكر الحقيقة كما هي ("لا توجد مراجعات مسجَّلة بعد"). نتيجة التشغيل: **1928 سؤالاً وجواباً آلياً موزَّعة على 439 بطاقة** (بين 2 و5 أسئلة لكل بطاقة حسب البيانات المتوفرة)، وصفر بطاقات بلا أي سؤال.

**بيانات FAQPage الهيكلية (JSON-LD):** كل صفحة تحمل الآن أيضاً كتلة `FAQPage` من نوع Schema.org في الـ`<head>` (دالة `faq_json_ld()` في `generate_listing_pages.py`) — هذا هو نفس النوع من البيانات الذي يمكن أن يجعل جوجل يعرض الأسئلة والأجوبة مباشرة في نتائج البحث (Rich Results)، ما يزيد فرصة الظهور والنقر.

**الآلية الدائمة التي طلبتها لإضافة أسئلة يدوية (لبطاقات حالية أو مستقبلية) دون إعادة العمل:**

أنشأت ملف Excel منفصل باسم `faq_additions.xlsx` (وأرسلت لك نموذجاً فارغاً جاهزاً `faq_additions_template.xlsx`) يحتوي على تبويبين:
1. **"Instructions"** — شرح خطوات الاستخدام.
2. **"FAQ Additions"** — جدول بأعمدة: `id` (معرّف البطاقة كما هو بالضبط في directory.json، أو معرّف بطاقة مستقبلية تخطط لإضافتها لاحقاً)، `title (reference only)` (للتذكير فقط، لا يُستخدم فعلياً)، `question`، `answer`. يمكنك إضافة عدة أسطر لنفس الـ`id` الواحد لعدة أسئلة.

عند وضع هذا الملف في نفس مجلد `build_directory.py` وإعادة تشغيل السكربت، يقرأ السكربت تلقائياً كل الأسئلة اليدوية ويُلحقها (وليس يستبدلها) بعد الأسئلة الآلية لكل بطاقة تطابق الـ`id`. **الملف اختياري تماماً** — إن لم يكن موجوداً، يستمر السكربت بالعمل عادياً بالأسئلة الآلية فقط (تحقّقتُ من هذا: الرسالة `no faq_additions.xlsx found (optional -- skipped)` تظهر بشكل طبيعي عند غيابه).

هذا يعني عملياً: أي وقت تريد إضافة خدمة أو بطاقة جديدة مستقبلاً، يمكنك أنت (أو من ينوب عنك) كتابة الفقرات والأسئلة الخاصة بها في هذا الملف الواحد بمعرّفها المستقبلي مسبقاً، وستُدمَج تلقائياً في أول عملية بناء تالية — دون الحاجة لطلب إعادة كتابة أي كود أو تكرار أي عمل سابق.

**التحقق:** فحصت 6 صفحات عيّنة من الفئات الأربع (منتِج، مهندس، مصمم، استوديوهين مختلفين) ببراوزر حقيقي (Playwright): قسم الأسئلة الشائعة يظهر بعنوانه الصحيح، بيانات `FAQPage` JSON-LD صالحة وغير فارغة في كل عيّنة، عدد الكلمات في الصفحة ارتفع من نطاق أقل إلى ما بين 205 و290 كلمة (بدون أي حشو مصطنع — الزيادة كلها محتوى حقيقي ومفيد)، وصفر أخطاء JavaScript.

كذلك تحقّقت من الصفحة الرئيسية (`index-preview.html`) بعد إعادة توليدها: الـ439 بطاقة تُعرض بشكل صحيح (21 منتج + 37 مهندس + 83 مصمم + 298 استوديو)، و439 رابط بطاقة يفتح صفحته الفردية الصحيحة تحت `listings/`، وصفر أخطاء JavaScript حقيقية (أخطاء الشبكة الظاهرة في الفحص هي بسبب قيود الشبكة في بيئة الاختبار السحابية نفسها فقط، وليست مشكلة في الموقع — كما وُثِّق سابقاً).

## تحديث: بريد احترافي حقيقي (contact@hiphoplord.com) وربط نموذج "List Your Service"

بعد إعداد بريد احترافي كامل الإرسال والاستقبال (`contact@hiphoplord.com`) عبر Hostinger — مع سجلات MX وSPF وDKIM وDMARC كلها مضبوطة بشكل صحيح على Cloudflare DNS ومُتحقَّق منها فعلياً (رسالة تجريبية أُرسلت واستُقبلت بنجاح) — حدّثت الموقع ليعكس ذلك:

- **`contact.html`:** استبدلت البريد الوهمي `support@hiphoplord.com` بالبريد الحقيقي `contact@hiphoplord.com` (كرابط `mailto:` قابل للنقر)، وحذفت ملاحظة "استبدل هذا البريد قبل النشر" لأنها لم تعد ضرورية.
- **نموذج "List Your Service" في `index.html`:** كان هذا النموذج معطلاً تماماً منذ البداية (يعرض تنبيهاً فقط عند الإرسال). ربطته الآن فعلياً بخدمة **Web3Forms** المجانية: أضفت حقول `name` لكل خانة (مطلوبة لإرسال البيانات)، حقل حماية بسيط من الروبوتات (`botcheck` مخفي)، واستبدلت منطق JavaScript بطلب `fetch` حقيقي إلى Web3Forms API يعرض حالة النجاح أو الفشل مباشرة للزائر بدل `alert()`.
- **مفتاح Web3Forms مُفعَّل بالكامل الآن:** سجّلت `contact@hiphoplord.com` في Web3Forms وحصلت على Access Key حقيقي، وتم إدخاله مباشرة في `index.html` (سطر `<input type="hidden" name="access_key" ...>` قرب بداية النموذج). نموذج "List Your Service" أصبح يعمل فعلياً الآن: أي زائر يرسل طلباً يصل مباشرة إلى صندوق `contact@hiphoplord.com`.

**لماذا هذا مهم لطلب AdSense:** موقع بنموذج معطل ظاهرياً وبريد وهمي لا يعطي انطباعاً احترافياً. الآن الموقع يملك قناة تواصل حقيقية وفعّالة بالكامل.

## تحديث: صفحة "Affiliate Disclosure" (إفصاح الروابط التابعة) — امتثال فعلي لمعايير FTC

طلبت إضافة صفحة إفصاح عن الروابط التابعة (Affiliate Disclaimer). قبل البناء بحثت في الإرشادات الرسمية للجنة التجارة الفيدرالية الأمريكية (FTC) الخاصة بالإفصاح عن الروابط التابعة، ووجدت نقطة مهمة جداً: **FTC تنص صراحة على أن وضع الإفصاح فقط في تذييل الموقع (Footer) أو في صفحة منفصلة يُعتبر غير كافٍ** — نص الإرشاد الحرفي: الإفصاحات "من المرجح أن يغفلها الزائر إذا ظهرت فقط في صفحة About أو في نهاية المنشور أو في أي مكان يتطلب من الشخص الضغط على 'المزيد'". الإفصاح يجب أن يكون قريباً من الرابط التابع نفسه، وقت رؤية الزائر له مباشرة.

بناءً على ذلك، لم أكتفِ بصفحة منفصلة كما طُلب حرفياً، بل نفّذت نظاماً بطبقتين لضمان الامتثال الفعلي:

- **صفحة `affiliate-disclosure.html` مستقلة:** بنفس القالب البصري لصفحتي Privacy Policy وTerms of Service تماماً (نفس التنقل، الخطوط، الألوان). تشرح بلغة بسيطة: ما هو الرابط التابع، أي الفئات تحتوي روابط تابعة فعلاً (المهندسون Engineers والمصممون Designers عبر Fiverr) وأيها لا تحتوي (المنتجون عبر SoundCloud، الاستوديوهات عبر مواقعها/Google — لا عمولة منها إطلاقاً)، تأكيد أن الاستقلالية التحريرية غير متأثرة بوجود الرابط التابع من عدمه، وسبب وجود هذا الإفصاح (الامتثال لـ FTC).
- **ملاحظة نصية مباشرة بجانب كل زر "View on Fiverr":** أضفت سطراً صغيراً "Affiliate link — disclosure" (برابط) يظهر تلقائياً تحت زر الدعوة للفعل مباشرة، لكن فقط على بطاقات فئتَي Engineers وVisuals تحديداً (الفئتين اللتين تحملان بالفعل `rel="sponsored"` من التدقيق السابق) — سواء في الصفحة الرئيسية (`index.html`) أو في كل صفحة فردية من صفحات القوائم الـ439. تحققت بالمتصفح الحقيقي (Playwright) أن العدد الظاهر فعلياً عند تحميل الصفحة هو **120 بطاقة بالضبط** (37 مهندساً + 83 مصمماً)، أي مطابق تماماً لعدد البطاقات التابعة الحقيقية — لا أكثر ولا أقل.
- **روابط الصفحة الجديدة أُضيفت في كل مكان منطقي:** تذييل الصفحة الرئيسية، تذييل كل صفحة قائمة فردية (عبر `FOOTER_HTML` في `generate_listing_pages.py`)، وضمن صفحتي Privacy Policy وTerms of Service (استبدلت فيهما أيضاً إشارة غير دقيقة لـ"BeatStars" لم تكن مستخدمة أصلاً في بياناتك الحقيقية).
- **`sitemap.xml`:** أضفت الصفحة الجديدة إلى قائمة الروابط الثابتة في `generate_sitemap()`، وأعدت توليد الخريطة بالكامل.
- **تواريخ "آخر تحديث":** استبدلت العنصر النائب `[insert date before publishing]` في كل من `privacy-policy.html` و`terms.html` بتاريخ حقيقي (19 أغسطس 2026)، وأضفت نفس الحقل لصفحة Affiliate Disclosure الجديدة.

**لماذا هذا مهم:** صفحة إفصاح منفصلة فقط قد تبدو كافية ظاهرياً لكنها لا تفي فعلياً بمعيار FTC، وقد تُعتبر لاحقاً نقطة ضعف عند مراجعة AdSense أو عند أي تدقيق قانوني. النظام الحالي (صفحة + إفصاح ملاصق للرابط نفسه) هو الممارسة المعتمدة فعلياً من كبار المسوّقين بالعمولة الملتزمين بالقانون.

## تحديث: ربط الدومين الفعلي بالموقع + حذف تنبيه "starter template" من الصفحتين القانونيتين

بعد أن أصبح الموقع يعمل فعلياً على `hiphoplord.com` و`www.hiphoplord.com` (عبر إضافة Custom Domain في إعدادات Cloudflare Worker، بعد حذف سجلات A قديمة كانت تُعيد التوجيه إلى صفحة "الدومين للبيع" الخاصة بـ GoDaddy)، طلبت حذف جملة تحذيرية كانت ظاهرة لكل زائر حقيقي في أسفل صفحتي `privacy-policy.html` و`terms.html`:

> "This is a starter template, not legal advice — have it reviewed before the site goes live..."

حذفت هذه الجملة من الصفحتين لأن الموقع أصبح مباشراً (live) فعلياً، فلم يعد من المناسب أن يرى الزوار تنبيهاً داخلياً كان موجهاً لك أثناء مرحلة البناء.

**تنبيه مهم لم يتغيّر رغم حذف الجملة:** محتوى صفحتي Privacy Policy وTerms لا يزال نصاً عاماً (قالباً جاهزاً) ولم تتم مراجعته من قِبل محامٍ مختص بقوانين بلدك. حذف الجملة التحذيرية هو إجراء تجميلي/احترافي فقط، ولا يُغني عن مراجعة قانونية حقيقية إذا رغبت بحماية كاملة لاحقاً — خصوصاً أن الموقع يجمع بيانات عبر نموذج ويحتوي روابط تابعة مدرّة للدخل.

## تحديث: ربط الموقع بـ Google Search Console وGoogle Analytics (GA4)

بعد أن أصبح الموقع مباشراً فعلياً على الدومين، ربطناه بأداتين أساسيتين من جوجل:

- **Google Search Console:** تحقّقت ملكية الدومين `hiphoplord.com` بنجاح (Domain property)، وقدّمنا `sitemap.xml` عبر قسم Sitemaps — الحالة ظهرت "Success" مع اكتشاف **445 صفحة** بالضبط (6 صفحات ثابتة + 439 صفحة فردية)، أي مطابقة تماماً لعدد صفحات الموقع الحقيقي. هذا يعني أن جوجل بدأ الآن في فهرسة كل صفحات الموقع تدريجياً.
- **Google Analytics 4 (GA4):** أنشأنا Property جديدة باسم "HipHopLord Directory" داخل حسابك الحالي في Google Analytics، مع Web Stream مربوط بـ `https://hiphoplord.com` والحصول على Measurement ID: `G-31TNRD57MQ`.

**التنفيذ في الكود:** أضفت كود تتبع Google (`gtag.js`) في وسم `<head>` مباشرة بعد فتحه، في **كل صفحة على الموقع بدون استثناء**:
- الصفحة الرئيسية `index.html`
- الصفحات الثابتة الخمس: `about.html`، `contact.html`، `privacy-policy.html`، `terms.html`، `affiliate-disclosure.html`
- قالب الصفحات الفردية في `generate_listing_pages.py` (وبالتالي كل الصفحات الـ439 المُولّدة منه تلقائياً بعد إعادة تشغيل السكربت)

تحققت بالمتصفح الحقيقي (Playwright) أن الكود يُحمَّل بدون أي أخطاء JavaScript، وأن الدالة `gtag` مُعرَّفة وجاهزة بمجرد فتح أي صفحة. بهذا، بمجرد نشر هذا التحديث على الموقع المباشر، ستبدأ Google Analytics بتسجيل الزيارات الحقيقية عبر كامل الموقع (439 صفحة فردية + الصفحات الثابتة)، وليس فقط الصفحة الرئيسية.

## ماذا فعلت بالضبط ببياناتك الأربعة

- **Producers (من SoundCloud):** الملفات الخام كانت عبارة عن مقاطع صوتية فردية وليست ملفات تعريف منتجين، وتحتوي على نسب كبيرة من محتوى غير مناسب (مثل حسابات "Hip Hop Classics" التي تعيد رفع أغاني قديمة مسجلة الحقوق، أو فنان مشهور فعلي مثل "Hungria Hip Hop" ينشر أغانيه الرسمية وليس بيتات للبيع). فرزت المقاطع حسب صاحب الحساب، واستبعدت الحسابات التي لا تحمل مؤشرات بيتميكينغ حقيقية (type beat, instrumental, prod. by...)، وأبقيت **4 منتجين حقيقيين** موثوقين مع مشغّل SoundCloud مدمج مباشرة في البطاقة.
- **Sound Engineers (من Fiverr):** كان الملف يضم عروضاً غير متعلقة بالهيب هوب إطلاقاً (بوب، فولك، موسيقى ألعاب). فلترت حسب نوع الموسيقى المعلن (hip_hop/rap/trap) وحسب كلمات مفتاحية هندسية (mix/master/vocal)، وأبقيت أفضل عرض واحد لكل بائع لتفادي الازدحام. النتيجة: **37 مهندس صوت** حقيقي، مع شارة تقييم Fiverr (★ ومستوى البائع) لكل بطاقة.
- **Visuals & Design (من Fiverr):** بيانات ممتازة وذات صلة كاملة تقريباً. دمجت الملفين، أزلت التكرار حسب البائع، واستخدمت حقل "design_style" (photographic / typographic / illustrative...) كوسوم بدل تكرار "hip hop" في كل بطاقة. النتيجة: **83 مصمماً**.
- **Studios (من Google Places):** بيانات دقيقة وحقيقية 100% (عناوين، هواتف، مواقع، تقييمات). استبعدت فقط النتائج المصنّفة خارج "Recording studio" (متاجر أسطوانات، مراكز أعمال...) وأزلت التكرار بين الملفين. النتيجة: **298 استوديو حقيقي** موزعة على أكثر من 20 مدينة أمريكية وبريطانية كبرى.

## الملفات

- **index.html** — الموقع، مع تحسينات جديدة: صورة مصغّرة (أو حرف أول بلون ذهبي كبديل) لكل بطاقة، شارة تقييم (★) للمهندسين والمصممين والاستوديوهات، عنوان وهاتف الاستوديو، مشغّل SoundCloud مدمج للمنتجين، وقسم "تصفح حسب المدينة" أصبح الآن يفلتر داخل الموقع مباشرة (24 مدينة، بدل فتح خرائط جوجل خارجياً).
- **directory.json** — قاعدة البيانات النهائية: 439 بطاقة حقيقية (21 منتج / 37 مهندس / 83 مصمم / 298 استوديو)، لا يوجد أي عنصر "SAMPLE".
- **build_directory.py** — السكربت الذي أنتج هذه البيانات، معاد كتابته بالكامل ليطابق الشكل الحقيقي لبيانات Apify (SoundCloud tracks / Fiverr gigs / Google Places)، وليس بنية مبسطة افتراضية كما في المحاولات السابقة. يمكنك إعادة تشغيله بنفسك لاحقاً إذا جلبت بيانات جديدة، بنفس بنية المجلدات.
- **about.html / contact.html / privacy-policy.html / terms.html** — محدّثة لتناسب المحتوى الحقيقي.
- **robots.txt / sitemap.xml** — جديدة، لتسهيل فهرسة الموقع في جوجل.
- **index-preview.html** — نسخة معاينة واحدة تحتوي كل البيانات مضمّنة داخلها (لغرض العرض فقط؛ النسخة الفعلية للنشر هي index.html + directory.json منفصلين).

## تحسينات السيو التي أضفتها

وسوم Open Graph وTwitter Card للمشاركة، رابط canonical في كل صفحة، بيانات JSON-LD (WebSite + ItemList) تشرح لجوجل طبيعة الموقع، نص alt وصفي لكل صورة، ملفا robots.txt وsitemap.xml.

**تنبيه مهني بخصوص السيو:** المحتوى الفعلي (439 بطاقة) يُحمَّل عبر JavaScript (fetch) وليس مكتوباً مباشرة في HTML. جوجل يزحف هذا النوع من المواقع وينفّذ الجافاسكريبت غالباً، لكنه أبطأ وأقل ضماناً من موقع تُبنى صفحاته مسبقاً (كما كنت تبحث سابقاً عبر Astro/Next.js). هذا اختيار مقصود حالياً حفاظاً على البساطة (بدون أدوات بناء)، وهو كافٍ للبداية، لكن إن أردت سيو أقوى مستقبلاً فالخطوة التالية المنطقية هي الانتقال لأداة توليد صفحات ثابتة.

## ما تحقق منه فعلياً

شغّلت الموقع بمتصفح حقيقي (Playwright) على البيانات الـ439 كاملة: 21/37/83/298 بطاقة بالضبط عند الفلترة حسب كل فئة، وتحققتُ مباشرة أن روابط Fiverr لكل بطاقة Engineers/Visuals مختلفة وصحيحة (وليست كلها تشير لرابط واحد كما كانت المشكلة الأصلية)، ولا توجد أي أخطاء JavaScript في الصفحة.

## تحديث: تحسينات تقنية بعد تحليل Semrush وHOTH (HTTPS، llms.txt، اتساق الكلمات المفتاحية)

بعد تحليل تقارير Semrush وHOTH (الماضي المشبوه للدومين، وضعف "اتساق الكلمات المفتاحية" الذي رصده تقرير HOTH ضمن تقييم GEO)، نفّذت ثلاثة تحسينات تقنية:

- **فرض HTTPS:** أضفت ملف `_headers` (ميزة خاصة بـ Cloudflare Pages تُقرأ تلقائياً عند النشر) يضيف ترويسة `Strict-Transport-Security` (HSTS) لكل صفحات الموقع، بحيث يفرض المتصفح استخدام HTTPS دائماً بعد أول زيارة آمنة. **تنبيه مهم:** هذا وحده لا يكفي — يجب أيضاً تفعيل خيار "Always Use HTTPS" من داخل لوحة تحكم Cloudflare (قسم SSL/TLS → Edge Certificates)، وهو إعداد على مستوى الدومين لا يمكن ضبطه من الكود؛ فعّله يدوياً إن لم يكن مفعّلاً (زر تبديل بسيط، أرشدك خطوة بخطوة إن احتجت).
- **ملف `llms.txt` جديد:** ملف نصي في جذر الموقع يشرح لمحركات الذكاء الاصطناعي (ChatGPT، Claude، Perplexity...) طبيعة الموقع وفئاته وصفحاته الأساسية بصيغة مباشرة قابلة للقراءة الآلية — أحد أهم توصيات تقرير HOTH ضمن تقييم GEO (Generative Engine Optimization).
- **اتساق الكلمات المفتاحية في صفحات القوائم الفردية (439 صفحة):** اكتشفت أن غالبية عناوين `<title>` وأوصاف meta لهذه الصفحات لم تكن تذكر كلمة "hip hop" أو الدور الوظيفي (منتج، مهندس مكساج، مصمم، استوديو) إطلاقاً — لأنها مبنية من بيانات خام (عنوان تراك SoundCloud فوضوي، أو وصف عرض Fiverr بأحرف صغيرة، أو اسم استوديو مجرّد). عدّلت `generate_listing_pages.py` بحيث يضيف تلقائياً كلمة مفتاحية ثابتة حسب الفئة (مثال: "Hip Hop Producer:"، "Recording Studio in [المدينة]") في بداية كل عنوان ووصف meta، دون أي تعديل على المحتوى الظاهر (ملاحظة المحرر) الذي تم التحقق منه سابقاً. كما استبدلت رمز "|" الحرفي الظاهر في 3 عناوين تراكات SoundCloud (كان يتسبب بعناوين تبدو وكأنها عدة عناوين مدمجة) بشرطة طويلة "–". أعدت توليد كل الصفحات الـ439 و`sitemap.xml`، وتحقّقت برمجياً أن كل صفحة تحتوي وسم `<title>` واحداً سليماً بلا أخطاء HTML.

## الخطوات التالية

1. **فعّل "Always Use HTTPS" في Cloudflare** (SSL/TLS → Edge Certificates) إن لم يكن مفعّلاً بعد — أخبرني وسأرشدك خطوة بخطوة.
2. ضع Fiverr Affiliate ID الحقيقي في أعلى build_directory.py (بدل "45990" التجريبي) وشغّله مرة أخيرة قبل النشر إن أردت.
3. اربط نموذج "List Your Service" بخدمة مثل Web3Forms أو Tally.
4. ارفع كل الملفات إلى مستودع GitHub واربطه بـ Cloudflare Pages (Framework preset: None، بلا build command) كما فعلت سابقاً.
5. راجع الصفحات القانونية وضع بريدك الإلكتروني الحقيقي في contact.html قبل تقديم طلب AdSense.

## تحديث: نظام "شارة" (badge) قابلة للتضمين لبناء روابط خلفية حقيقية من الاستوديوهات

بعد نقاش حول فكرة من فيديو يقارن بين WordPress والبرمجة بالذكاء الاصطناعي لبناء المواقع الدليلية، نفّذت نسخة آمنة من فكرة "الشارة القابلة للتضمين" (badge widget) التي يستخدمها بعض أصحاب الدلائل لبناء روابط خلفية حقيقية.

**كيف تعمل:** كل صفحة من صفحات الاستوديوهات الـ298 فقط (وليس المنتجين أو المهندسين أو المصممين) تعرض الآن قسماً جديداً بعنوان "Is This Your Studio?" يحتوي شارة SVG بسيطة بألوان الموقع (`badge.svg` في جذر الموقع) وكود HTML جاهز للنسخ يمكن لصاحب الاستوديو لصقه في موقعه الخاص، يشير مباشرة إلى صفحته الفردية على HipHopLord.

**قرار مهم بخصوص الأمان القانوني/سياسات جوجل:** الفيديو الأصلي يربط الشارة بمقابل مادي (شهر مجاني من "Featured Listing" مقابل الرابط) — وهذا تحديداً نوع الروابط التي تحذّر منها إرشادات Google الرسمية بخصوص "Link Schemes" (روابط مقابل منتج أو خدمة أو أي مقابل). لتفادي هذا الخطر تماماً — خصوصاً أن الدومين لديه بالفعل تاريخ مضطرب مع الروابط كما أظهر تحليل Semrush — جعلت العرض هنا **مجانياً بالكامل وبلا أي مقابل أو شرط**: مجرد "خذها إذا أردت"، دون ربطها بأي ترقية أو ميزة مدفوعة. هذا يبقي الفكرة ضمن الممارسات الآمنة والمقبولة من جوجل (رابط اختياري وطوعي من نشاط تجاري حقيقي).

**ملف التواصل الجديد `hiphoplord_studio_outreach.xlsx`:** يحتوي قائمة بالاستوديوهات الـ298 (الأعلى تقييماً ومصنّفة "مميزة" أولاً، وعددها 66)، مع الهاتف والموقع ورابط الصفحة الفردية وقناة التواصل المقترحة، بالإضافة إلى نص جاهز جاهز (سكربت اتصال هاتفي + رسالة لإرسالها عبر نموذج تواصل الموقع) في ورقة "Message Templates". بما أن بيانات Google Places لا تتضمن أي بريد إلكتروني (0 من 298)، فالقناتان الواقعيتان للتواصل هما الهاتف (متوفر لـ282 استوديو) أو نموذج التواصل على موقع الاستوديو نفسه (متوفر عملياً لـ272 استوديو؛ الباقي رابط خرائط جوجل فقط بلا موقع حقيقي).

**تنبيه:** أرسل رسائل مخصصة لكل استوديو (بالاسم والمدينة) بدل نسخ رسالة مطابقة للجميع دفعة واحدة — هذا مذكور في ملف التواصل نفسه، ومهم لتجنّب أن يبدو التواصل كرسائل مزعجة (spam) وليس فقط لتحسين معدل الاستجابة.

**تحديث لاحق — نقل موضع القسم:** بناءً على مراجعة تجربة المستخدم، نقلت قسم "Is This Your Studio?" من موضعه الأصلي (ملاصقاً لزر الحجز مباشرة) إلى أسفل الصفحة (بعد قسم "More in [الفئة]"، قبل الفوتر) — بنفس منطق "Claim your business" في Yelp وGoogle Business: محتوى إداري موجّه لصاحب النشاط لا يجب أن يقاطع رحلة قرار الزائر الأساسي (الفنان الباحث عن استوديو). أعطيته أيضاً نفس تصميم "البطاقة" (card) المستخدم في بقية أقسام الصفحة ليبدو قسماً متكاملاً لا مبتوراً من سياقه.

## تحديث: خلفية متحركة بـCSS/SVG خالصة في القسم الرئيسي (Hero) — بلا أي تكلفة على سرعة التحميل

بدل اقتراح فيديو خلفية (يبطئ الموقع فعلياً ويؤثر على مقياس LCP أحد معايير Core Web Vitals)، أضفت تأثيرين بصريين مستوحيين من عالم الهيب هوب، بـCSS وSVG خالصين بدون أي صورة أو فيديو أو طلب شبكة إضافي:

- **حلقات اسطوانة (vinyl) دوّارة خلف العنوان الرئيسي:** SVG دوائر متحدة المركز بلون ذهبي شفاف جداً (opacity 7%)، تدور ببطء شديد (دورة كاملة كل 60 ثانية) خلف نص "The Underground Hip Hop Production Directory" — إشارة مباشرة لعالم الديجي/الإنتاج الموسيقي.
- **أشرطة إيكولايزر (equalizer) نابضة فوق شريط البحث مباشرة:** 12 شريطاً ذهبياً رفيعاً بارتفاعات متفاوتة تتحرك أعلى وأسفل بشكل عشوائي المظهر (بتوقيت وتأخير مختلف لكل شريط)، بشفافية منخفضة (16%) — إشارة مباشرة لموجات الصوت/الميكساج.

كلا التأثيرين: بدون أي ملف يُحمَّل (SVG وCSS مضمّنان مباشرة في الصفحة)، بلا أي تأثير على سرعة التحميل أو Core Web Vitals، ومعطّلان تلقائياً لمن يفعّل "Reduce Motion" في نظام تشغيله (`prefers-reduced-motion`)، ومنخفضا الشفافية بشكل مقصود بحيث يبقيان خلفية جمالية بحتة لا تنافس النص أو زر البحث. طُبِّق التعديل على `index.html` وأعيد توليد `index-preview.html` تبعاً له، وتحققت عبر Playwright من عمل الصفحة بصفر أخطاء JavaScript وظهور كل الـ439 بطاقة بشكل طبيعي.

## تحديث: شعار كتابي (wordmark) جديد — حرف الـO في "LORD" بشكل اسطوانة فينيل + أيقونة favicon حقيقية

بعد نقاش حول خيارات الشعار، اخترت اتجاه "الشعار الكتابي البسيط بدون أيقونة منفصلة": استبدال حرف الـO في "LORD" بعلامة صغيرة (حلقة ذهبية + نقطة مركزية) تشبه ثقب اسطوانة الفينيل، بدل حرف O عادي — بنفس لغة الشكل المستخدمة في حلقات الفينيل المتحركة بالقسم الرئيسي، بحيث يصبح الاسم نفسه هو الشعار دون أيقونة إضافية منفصلة.

**التنفيذ:** SVG بسيط جداً (حلقة + نقطة، بلون `currentColor` ليرث اللون الذهبي تلقائياً) يحل محل حرف الـO داخل النص، بحجم متناسب مع باقي الأحرف (`0.62em`) ومحاذاة دقيقة للخط الأساسي. طُبِّق هذا على شعار شريط التنقل في كل صفحات الموقع بلا استثناء: الصفحة الرئيسية، الصفحات الثابتة الخمس، وقالب الصفحات الفردية الـ439 (عبر `NAV_HTML` في `generate_listing_pages.py`) — كلها تشترك الآن بنفس الشعار الموحّد. أضفت أيضاً `aria-label="HipHopLord"` على رابط الشعار حتى يبقى مقروءاً بشكل صحيح لبرامج قراءة الشاشة رغم أن النص المرئي أصبح مقسّماً بين نص وSVG.

**أيقونة favicon حقيقية لأول مرة:** استبدلت الأيقونة المؤقتة (إيموجي سماعات 🎧 كانت موجودة فقط في الصفحة الرئيسية وصفحات القوائم) بنفس علامة "حلقة الفينيل" على خلفية دائرية داكنة، وأضفتها الآن أيضاً للصفحات الخمس الثابتة التي لم تكن تملك أي favicon أصلاً (About، Contact، Privacy Policy، Terms، Affiliate Disclosure) — أول أيقونة متسقة وحقيقية للموقع عبر كل صفحاته دون استثناء. اختبرت العلامة بصرياً بحجمي 32 و16 بكسل (أحجام تبويب المتصفح الفعلية) وتبقى واضحة ومقروءة في الحجمين.

تحققت برمجياً (HTMLParser) أن كل الملفات الـ446 (الصفحات الثابتة + الصفحات الفردية + المعاينة) سليمة البنية بدون أي خطأ HTML، وأن الإيموجي القديم اختفى بالكامل من كل الملفات، وعبر Playwright أن جافاسكريبت الموقع يعمل بصفر أخطاء بعد التعديل.

## تحديث: قسم "Guides" جديد — 8 مقالات إرشادية لاستهداف كلمات مفتاحية معلوماتية

أضفت قسماً جديداً بعنوان "Guides" (وليس "Blog" عمداً — لا يظهر بهذا الاسم في أي مكان بالواجهة، ولا يُبنى على شكل مدونة بمنشورات متتالية بتاريخ نشر) يستهدف كلمات مفتاحية معلوماتية (informational search intent) لا تغطيها صفحات القوائم الـ439 أصلاً. صفحة قائمة فردية ترتّب لعبارة مثل "مهندس مكساج في أتلانتا"؛ مقال إرشادي يرتّب لعبارة مثل "كم تكلفة المكساج والماسترنج" — كلمات يبحث عنها فنانون مستقلون قبل أن يعرفوا أصلاً أنهم بحاجة لصفحة قائمة معينة.

**العناوين الثمانية المُنجزة** (كل واحد بصفحة كاملة تحت `/guides/`):

1. Type Beat vs. Custom Beat: Which One Should You Actually Buy?
2. How Much Does a Hip Hop Beat Cost in 2026? A Realistic Price Guide
3. How Much Does Mixing and Mastering Cost for a Hip Hop Track?
4. Mixing vs. Mastering: What's the Difference, and Do You Need Both?
5. What Makes Good Hip Hop Cover Art? A Guide for Independent Artists
6. How to Choose a Recording Studio for Your First Hip Hop Session
7. Best US Cities to Record Hip Hop Music in 2026
8. How to Release Your First Hip Hop Track: A Step-by-Step Guide for Independent Artists

**أسلوب الكتابة:** كتبت كل مقال بنفسي بأسلوب بشري يعكس إلماماً حقيقياً بثقافة الهيب هوب ومصطلحاتها (type beat، lease، trackout/stems، exclusive rights، chopped and screwed، drill، boom bap...) — وليس محتوى عاماً مُعاد صياغته. كل رقم مذكور في المقالات (نطاقات أسعار المهندسين والمصممين، متوسط السعر، عدد المدن، أكثر المدن ظهوراً بين الاستوديوهات) مسحوب برمجياً ومباشرة من `directory.json` وقت التوليد (دالة `compute_stats()`) — لا يوجد رقم واحد مُختلَق، بنفس المعيار الذي التزم به الموقع منذ البداية. المقال السابع (أفضل المدن للتسجيل) يتضمن أيضاً سياقاً ثقافياً/تاريخياً حقيقياً لكل مدينة (ممفيس وامتداد Three 6 Mafia/horrorcore، أتلانتا كمركز الـtrap، هيوستن وأسلوب chopped and screwed، شيكاغو ومولد الـdrill، إلخ).

**الربط بين المقالات (حسب طلبك):** كل مقال ينتهي بقسم "More Guides" — بطاقات لـ3-4 مقالات أخرى ذات صلة مباشرة، بنفس تصميم بطاقات الموقع. أضفت أيضاً روابط داخلية طبيعية *داخل* نص كل مقال (وليس فقط في الأسفل) — مثلاً مقال "كيف تُصدر أول تراك" يربط كل خطوة بالمقال المتخصص فيها مباشرة أثناء الشرح، لا كقائمة روابط مقحمة.

**صفحة فهرس جديدة `/guides/index.html`:** بطاقات الثمانية مقالات في شبكة واحدة، تُستخدم كمرجع من رابط "Guides" الجديد في فوتر كل صفحات الموقع (وليس القائمة الرئيسية العلوية، كما اتفقنا سابقاً حتى لا يُقرأ الموقع كمدونة).

**السيو التقني لكل صفحة:** بيانات JSON-LD منفصلة لكل مقال (`Article` + `BreadcrumbList`)، بالإضافة إلى `FAQPage` (3 أسئلة شائعة حقيقية لكل مقال، مبنية على الأسئلة الفعلية التي يطرحها فنانون مستقلون)، meta description مخصص، رابط canonical، وسوم Open Graph/Twitter. أضفت أيضاً 8 روابط `/guides/*.html` + رابط `/guides/` إلى `sitemap.xml` (المجموع الآن 453 رابطاً)، وأضفت فئة CSS جديدة `.prose-guide` في `index.html` (تُطبَّق تلقائياً على كل صفحات القسم عبر `load_shared_style()`) لتنسيق نص المقالات (عناوين فرعية، فقرات، قوائم نقطية بعلامة ذهبية) بما يتماشى مع الهوية البصرية الداكنة/الذهبية لبقية الموقع.

**الملف الجديد:** `generate_guides.py` (سكربت مستقل، يستورد الثوابت المشتركة من `generate_listing_pages.py` مثل شريط التنقل والفوتر والستايل بدل تكرارها). شغّلته بعد `generate_listing_pages.py` مباشرة (الترتيب مهم: الأول يبني صفحات القوائم ويكتب sitemap.xml أولياً، والثاني يبني صفحات Guides ثم يعيد كتابة sitemap.xml نهائياً ليشمل الاثنين معاً). تحققت برمجياً من: خلو كل الملفات التسعة (8 مقالات + الفهرس) من أخطاء HTML، صحة بنية JSON لكل بيانات JSON-LD (Article وBreadcrumbList وFAQPage)، خلو كود جافاسكريبت GA4 من أخطاء صياغية، وظهور قسم "More Guides" في كل مقال بالروابط الصحيحة. كما التقطت لقطة شاشة لصفحة الفهرس عبر بيئة اختبار معزولة (نظراً لقيود الشبكة في بيئة العمل التي تمنع الوصول لـTailwind CDN مباشرة) للتأكد بصرياً من أن التصميم يطابق باقي الموقع.
